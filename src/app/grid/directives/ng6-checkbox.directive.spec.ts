import { Ng6CheckboxDirective } from './ng6-checkbox.directive';

describe('Ng6CheckboxDirective', () => {
  it('should create an instance', () => {
    const directive = new TableCheckboxDirective();
    expect(directive).toBeTruthy();
  });
});
